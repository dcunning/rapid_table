# frozen_string_literal: true

RSpec.describe RapidTable do
  it "has a version number" do
    expect(RapidTable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
